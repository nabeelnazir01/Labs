#include <stdio.h>
#include <stdlib.h>
#include <string.h>
    int main()
{
    setvbuf(stdout, NULL, _IONBF, 0);
    setvbuf(stderr, NULL, _IONBF, 0);
    struct {
        char fName[19];
        char lName[19];
        int studID;
        *student;
    } student;

        struct {
        char code[7];
        char name[24];
    } course;
}